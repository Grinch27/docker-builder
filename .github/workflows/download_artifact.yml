name: Download File to Artifact

on:
  workflow_dispatch:
    inputs:
      file_url:
        description: "URL to download"
        type: string
        required: true
        default: "https://dl.google.com/chrome/install/ChromeStandaloneSetup64.exe"

run-name: Download File to Artifact

jobs:
  download:
    name: Download File to Artifact
    runs-on: ubuntu-latest
    permissions: write-all
    defaults:
      run:
        shell: bash
    container:
      image: ubuntu:devel
      # 按官方文档允许的字段，尽量给满 GHA 可传递的容器运行权限
      options: >-
        --privileged
        --user=0:0
        --pid=host
        --ipc=host
        --uts=host
        --cgroupns=host
        --cap-add=ALL
        --security-opt apparmor=unconfined
        --security-opt seccomp=unconfined
      volumes:
        - /dev:/dev
        - /sys:/sys:ro
        - /sys/fs/cgroup:/sys/fs/cgroup
        - /run/udev:/run/udev:ro
        - /lib/modules:/lib/modules:ro
    env:
      DEBIAN_FRONTEND: noninteractive
      FORCE_UNSAFE_CONFIGURE: "1"
      # ========= Workspace Environment Variables ==========
      compress: true
      PATH_output: "/output"
    steps:
      - name: Masked file URL
        id: mask-url
        uses: actions/github-script@main
        with:
          script: |
            const fileUrl = context.payload.inputs.file_url
            core.setSecret(fileUrl)
            core.exportVariable('FILE_URL', fileUrl)

      # - name: Set File URL
      #   run: |
      #     echo -e "::add-mask::${{ github.event.inputs.file_url }}"
      #     echo -e "FILE_URL=${{ github.event.inputs.file_url }}" >> $GITHUB_ENV

      - name: Install Tools
        id: apt-download
        working-directory: /
        run: |
          run_root() {
            if command -v sudo >/dev/null 2>&1; then
              sudo "$@"
            else
              "$@"
            fi
          }
          run_root apt-get -yqq update
          run_root apt-get -yqq install ca-certificates curl xz-utils
          curl --version
          xz --version

      - name: Download
        id: file-download
        working-directory: /
        run: |
          mkdir -p ${{ env.PATH_output }}
          cd "${{ env.PATH_output }}"

          echo -e "Downloading file"
          FILE_NAME="$(curl -fsSLJO -w '%{filename_effective}' "${FILE_URL}")"
          if [[ -z "${FILE_NAME}" || ! -f "${FILE_NAME}" ]]; then
            echo -e "::error::Unable to determine downloaded file name"
            exit 1
          fi

          echo -e "::add-mask::$FILE_NAME"
          FILE_EXT="${FILE_NAME##*.}"
          if [[ "${FILE_NAME}" == "${FILE_EXT}" ]]; then
            echo -e "::error::Unable to detect file extension from ${FILE_NAME}"
            exit 1
          fi

          {
            echo -e "FILE_NAME=${FILE_NAME}"
            echo -e "FILE_EXT=${FILE_EXT}"
          } | tee -a "${GITHUB_ENV}" "${GITHUB_OUTPUT}"

      - name: Compress
        id: file-compress
        uses: Grinch27/github-actions/.github/actions/compress-xz@main
        with:
          path_target: ${{ env.PATH_output }}
          input_ext: ${{ steps.file-download.outputs.FILE_EXT }}
          split_size: "1999M"
          split_threshold: "2000000000"
          mem_hold: "2048"
          memlimit: "4GiB"

      - name: Upload Artifact
        id: file-artifact
        uses: actions/upload-artifact@main
        with:
          name: ${{ steps.file-download.outputs.FILE_NAME }}
          path: "${{ env.PATH_output }}/*.${{ steps.file-compress.outputs.target_ext }}*"
          retention-days: 1
